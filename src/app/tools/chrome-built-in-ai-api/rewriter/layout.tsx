import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'AI Rewriter: Enhance & Rewrite Text with Chrome Built-in AI',
  description:
    "Rewrite and improve your text instantly using Chrome's built-in AI. Get better clarity, tone, and structure effortlessly. Try it now!",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
