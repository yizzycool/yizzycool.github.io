import { Metadata } from 'next';

export const metadata: Metadata = {
  title:
    'Chrome Built-in AI: Language Detector API – Instantly Identify Languages',
  description:
    "Experience Chrome's built-in AI Language Detector API. Detect languages instantly with browser-native AI—no extra libraries required. Try it online now!",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
