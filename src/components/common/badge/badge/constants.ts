import type { BadgeVariant, BadgeSize } from '@/types/common/badge';
import type { Rounded } from '@/types/common';

export const DEFAULT_BADGE_VARIANT: BadgeVariant = 'neutral';
export const DEFAULT_BADGE_SIZE: BadgeSize = 'xs';
export const DEFAULT_BADGE_ROUNDED: Rounded = 'full';
