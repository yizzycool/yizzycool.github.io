import type { CountBadgeVariant, CountBadgeSize } from './types';

export const DEFAULT_COUNT_BADGE_VARIANT: CountBadgeVariant = 'subtle';
export const DEFAULT_COUNT_BADGE_SIZE: CountBadgeSize = 'xs';
