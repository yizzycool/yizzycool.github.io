import type { Rounded } from '@/types/common';
import type { ButtonSize, ButtonVariant } from '@/types/common/button';

export const DEFAULT_BUTTON_VARIANT: ButtonVariant = 'primary';
export const DEFAULT_BUTTON_SIZE: ButtonSize = 'base';
export const DEFAULT_BUTTON_ROUNDED: Rounded = 'lg';
export const DEFAULT_BUTTON_BORDERED: boolean = false;
export const DEFAULT_BUTTON_HOVER_EFFECT: boolean = true;
export const DEFAULT_BUTTON_DISABLED: boolean = false;
export const DEFAULT_BUTTON_ICON_STROKE_WIDTH: number = 2;
