import type { Animation, Rounded } from '@/types/common';
import type { SurfaceVariant } from '@/types/common/surface';

export const DEFAULT_SURFACE_VARIANT: SurfaceVariant = 'default';
export const DEFAULT_SURFACE_ROUNDED: Rounded = 'xl';
export const DEFAULT_SURFACE_HOVER_EFFECT: boolean = true;
export const DEFAULT_SURFACE_BORDERED: boolean = true;
export const DEFAULT_SURFACE_ANIMATION: Animation = 'none';
